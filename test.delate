bracken -d /scratch/qou/database/kraken2 -i Q54-25S-3-11_kraken2.report -o rest.output.bracken_report_D -r 150 -l S -t 2


        mmseqs easy-cluster {all_samples_protein_merged.fasta} \
                           {params.prefix} \
                           {params.tmp_dir} \
                           --min-seq-id {params.min_seq_id} \
                           -c {params.coverage} \
                           --threads {params.threads} 


        mmseqs easy-lincluster all_samples_protein_merged.fasta \
    clustered_protein \
    tmp_protein \
    --min-seq-id 0.9 \
    --split-memory-limit 1000M\
    -c 0.9 \
    --threads 4




     python /scratch/qou/2nd/script/KrakenTools/combine_kreports.py  -r *.report -o ../species.report